apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "nginx"
  name: web-ingress-test
  namespace: jc-dev
spec:
  tls:
  - hosts:
    - naythanc.dev
    secretName: jc-dev-tls
  rules:
  - host: naythanc.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: cat-facts
            port: 
              number: 8080